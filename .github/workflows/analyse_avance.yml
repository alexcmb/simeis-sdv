name: analyse-avancé

on:
  merge:
    branche:[release/*]

jobs:
  quality:
    runs-on: ubuntu-latest

    steps:
      - name: Cargo unit test
        run: make unit_test

      - name: Check Python formatting
        run: python3 test/unit-test.py

      - name: audit rust
        run: make audit