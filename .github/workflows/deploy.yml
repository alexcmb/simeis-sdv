#WILLIAM
name: deploy new release
on:
  push:
jobs:
  ssh:
    name: connexion-ssh
    runs-on: ubuntu-latest
    container:
      image: ubuntu:latest
    steps:
      - name: connexion ssh
        run: ssh ssh -i "${{ secrets.SSH_WILLIAM }}" -p 22222 william@78.123.114.124
      